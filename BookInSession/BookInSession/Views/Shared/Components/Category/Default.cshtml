@model BookInSession.ViewModels.CategoryListViewModel
<ul class="list-group">
    <li class="list-group-item">
     <a asp-controller="Home" asp-action="Index" style="text-decoration: none; color: black;">
        Tüm Kitaplar
     </a>
    </li>
    @foreach (var category in Model.Categories)
    {
        <li class="list-group-item @(category.Id == Model.CurrentCategory ? "active":"")">
            <a href="/home/index?categoryId=@category.Id" style="text-decoration: none; color: black;">
                @category.CategoryName
            </a>
        </li>
    }
</ul>